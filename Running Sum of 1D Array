class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        
        vector<int> max;
        int sum = 0;
        for(int i=0;i<nums.size();i++)
        {
            sum+=nums[i];
            max.push_back(sum);
        }
        return max;
    }
};
